// URL web app POP-AI sau khi bật GitHub Pages (Settings → Pages → Branch: main → Save).
// Dạng chuẩn: https://<tên-tài-khoản-github>.github.io/<tên-repo>/
// (có dấu "/" ở cuối, và bao gồm cả /<tên-repo>/ nếu đây là project site chứ không phải user site).
// Nếu dùng domain riêng (CNAME), thay bằng domain đó.
export const APP_URL = "https://TEN-TAI-KHOAN.github.io/pop-ai/";
