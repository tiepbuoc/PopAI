{
  "name": "pop-ai-functions",
  "description": "Cloud Functions cho POP-AI — trợ lý AI cá nhân hóa (gọi LLM, giấu API key phía server)",
  "engines": { "node": "20" },
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.1"
  },
  "private": true
}
